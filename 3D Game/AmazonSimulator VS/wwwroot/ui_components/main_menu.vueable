<div>
    % @click="toggle_visibility()" =>
    <button>toggle visible</button>
    % @click="add_one()" =>
    <button>add one</button>
    % v-if="is_visible" =>
    <div>
        % v-for="i in _.range(test3.length)" =>
        <h1 style="background-color: aquamarine">
            some text {{i + 1}}
        </h1>
    </div>

</div>
